
const authConfig = {
    providers: [
        {
            domain: "https://relaxed-camel-85.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;